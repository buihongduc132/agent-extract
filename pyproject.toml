[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-extract"
version = "0.1.0"
description = "Unified extractor for OpenCode and Codex JSONL output"
requires-python = ">=3.12"

[tool.pytest.ini_options]
addopts = "-q"
